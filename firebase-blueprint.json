{
  "entities": {
    "profile": {
      "title": "User Profile",
      "description": "Professional profile and philosophy of the user",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "bio": { "type": "string" },
        "philosophy": { "type": "string" },
        "roles": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["name", "bio", "philosophy"]
    },
    "project": {
      "title": "Project Context",
      "description": "Business or personal project information and assets",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" },
        "logos": { "type": "array", "items": { "type": "string" } },
        "trainingMaterial": { "type": "array", "items": { "type": "string" } },
        "driveContext": { 
          "type": "array", 
          "items": { 
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "content": { "type": "string" }
            }
          }
        },
        "methodology": { "type": "string", "enum": ["SPE", "DE", "Custom"] },
        "ownerId": { "type": "string" }
      },
      "required": ["name", "ownerId"]
    },
    "saved_asset": {
      "title": "Saved Creative Asset",
      "description": "Finalized design from the Creative Engine",
      "type": "object",
      "properties": {
        "ownerId": { "type": "string" },
        "imageUrl": { "type": "string" },
        "strategy": { "type": "string" },
        "format": { "type": "string" },
        "style": { "type": "string" },
        "brandName": { "type": "string" },
        "createdAt": { "type": "object" }
      },
      "required": ["ownerId", "imageUrl", "brandName"]
    },
    "product": {
      "title": "Product Offer",
      "description": "Payment and billing products configured per user account",
      "type": "object",
      "properties": {
        "productName": { "type": "string" },
        "productDescription": { "type": "string" },
        "productPrice": { "type": "number" }
      },
      "required": ["productName", "productPrice"]
    },
    "publication": {
      "title": "Scheduled Publication",
      "description": "Content scheduled to be published on social media channels",
      "type": "object",
      "properties": {
        "ownerId": { "type": "string" },
        "title": { "type": "string" },
        "copy": { "type": "string" },
        "scheduledTime": { "type": "string" },
        "channels": { "type": "array", "items": { "type": "string" } },
        "imageUrl": { "type": "string" },
        "status": { "type": "string" },
        "createdAt": { "type": "string" }
      },
      "required": ["ownerId", "title", "copy", "scheduledTime"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "profile",
      "description": "Public and private user profile data"
    },
    "users/{userId}/products/{productId}": {
      "schema": "product",
      "description": "Custom payment products of the user profile"
    },
    "projects/{projectId}": {
      "schema": "project",
      "description": "Project context and brand guidelines"
    },
    "saved_assets/{assetId}": {
      "schema": "saved_asset",
      "description": "Finalized designs saved in the Gallery"
    },
    "publications/{pubId}": {
      "schema": "publication",
      "description": "Scheduled content pieces on the marketing calendar"
    }
  }
}
